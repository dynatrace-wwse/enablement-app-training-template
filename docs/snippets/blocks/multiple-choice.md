```markdown
<!-- LAB_QUESTION
type: multiple-choice
question: "What does `kubectl rollout restart` do?"
options:
  - "Replaces the pods gradually — new pods start before old ones terminate"
  - "Deletes every pod in the namespace immediately"
  - "Reloads the Kubernetes API server configuration"
correct: 0
explanation: "A rolling restart keeps the application available throughout."
-->
```
